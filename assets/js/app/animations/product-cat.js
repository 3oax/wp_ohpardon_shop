/* eslint new-cap: [0] */
/* eslint one-var: [0] */
/* eslint max-len: [0] */

import Utils from '../utils';
import Fixes from '../fixes.js';

const AnimationProductCat = {
	enter( args ){
		// console.log(args);
	},

	initViewportFx( container ){
		
	},

};

export default AnimationProductCat;
